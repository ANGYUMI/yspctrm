void setup() {
  size(100, 100, P3D);
  noStroke();
} 

void draw() {
  background(0);
  pointLight(255, 255, 255, mouseX, mouseY, mouseX);
   translate(width/2, height/2, 0);
  sphere(30);
  translate(width/2, height/2, 0);

}
